{% if page.handle == 'bodycoverageperfector-exclusiveoffer' %}
  {% render 'westmore-exclusive' %}
{% else %}
  <main class="page-width" role="main">
    <h1>{{ page.title }}</h1>
    {{ page.content }}
  </main>
{% endif %}
